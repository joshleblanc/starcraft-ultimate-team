<% password_digest = BCrypt::Password.create("password") %>

one:
  email_address: one@example.com
  password_digest: <%= password_digest %>
  password_reset_token: <%= Rails.application.message_verifier("password_reset").generate(298486374) %>

two:
  email_address: two@example.com
  password_digest: <%= password_digest %>
  password_reset_token: <%= Rails.application.message_verifier("password_reset").generate(298486375) %>
